# DoliDemo — Dolibarr 23.0.3 + réalistic demo data, ready for Coolify
# ("Docker Compose" build pack, GitHub App source).
#
# Coolify fills the SERVICE_* magic variables automatically and routes the
# public domain to the service that declares it (dolibarr, port 80).
# Local run:  docker compose up -d  →  http://localhost:8090 (admin / admin)

services:
  dolibarr:
    build: .
    restart: unless-stopped
    ports:
      - "${DOLIDEMO_PORT:-8090}:80"
    environment:
      # Coolify magic vars: SERVICE_FQDN_DOLIBARR_80 binds the domain to port 80,
      # SERVICE_URL_DOLIBARR / SERVICE_USER_* / SERVICE_PASSWORD_* are generated.
      SERVICE_FQDN_DOLIBARR_80: ${SERVICE_FQDN_DOLIBARR_80:-}
      DOLI_DB_HOST: mariadb
      DOLI_DB_NAME: dolibarr
      DOLI_DB_USER: ${SERVICE_USER_MYSQL:-dolibarr}
      DOLI_DB_PASSWORD: ${SERVICE_PASSWORD_MYSQL:-dolibarr}
      DOLI_ADMIN_LOGIN: ${SERVICE_USER_DOLIBARR:-admin}
      DOLI_ADMIN_PASSWORD: ${SERVICE_PASSWORD_DOLIBARR:-admin}
      DOLI_URL_ROOT: ${SERVICE_URL_DOLIBARR:-http://localhost:8090}
      DOLI_COMPANY_NAME: ${DOLI_COMPANY_NAME:-DoliDemo}
      DOLI_CRON: 0
      DOLI_CRON_KEY: ${SERVICE_PASSWORD_CRONKEY:-dolidemo-cron-key}
      DOLI_INIT_DEMO: 0
      DOLI_INIT_DEMO_REALISTIC: 1
      DOLI_ACTIVATE_MODULES: modCron,modDoliModuleManager
      DOLI_MODULE_ADDON_DIR_CUSTOM: 1
      PHP_INI_DATE_TIMEZONE: Europe/Paris
    depends_on:
      mariadb:
        condition: service_healthy
    volumes:
      - dolibarr_documents:/var/www/documents
      - dolibarr_custom:/var/www/html/custom
      - dolibarr_conf:/var/www/html/conf
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:80"]
      interval: 10s
      timeout: 10s
      retries: 30

  # Pings the public cron endpoint every minute (Dolibarr scheduled jobs).
  dolibarr-cron:
    image: curlimages/curl:latest
    restart: unless-stopped
    depends_on:
      dolibarr:
        condition: service_healthy
    environment:
      CRON_KEY: ${SERVICE_PASSWORD_CRONKEY:-dolidemo-cron-key}
      ADMIN_LOGIN: ${SERVICE_USER_DOLIBARR:-admin}
    entrypoint: /bin/sh
    command:
      - -c
      - |
        while true; do
          curl -fsS -m 300 -o /dev/null \
            "http://dolibarr/public/cron/cron_run_jobs_by_url.php?securitykey=$${CRON_KEY}&userlogin=$${ADMIN_LOGIN}" \
            && echo "[$$(date -Iseconds)] cron tick ok" \
            || echo "[$$(date -Iseconds)] cron tick failed"
          sleep 60
        done

  mariadb:
    image: mariadb:11
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: dolibarr
      MYSQL_USER: ${SERVICE_USER_MYSQL:-dolibarr}
      MYSQL_PASSWORD: ${SERVICE_PASSWORD_MYSQL:-dolibarr}
      MYSQL_ROOT_PASSWORD: ${SERVICE_PASSWORD_ROOT:-root}
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 20s
      retries: 20

  # SMTP sink: emails sent from the demo land here instead of real inboxes.
  # Configure Dolibarr: MAIN_MAIL_SENDMODE=smtps, MAIN_MAIL_SMTP_SERVER=mailpit, port 1025.
  mailpit:
    image: axllent/mailpit:latest
    restart: unless-stopped
    environment:
      SERVICE_FQDN_MAILPIT_8025: ${SERVICE_FQDN_MAILPIT_8025:-}
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1

volumes:
  dolibarr_documents:
  dolibarr_custom:
  dolibarr_conf:
  mariadb_data:
